#include "main.h"
/**
 * free_array - free an array
 * @a: array to be freed
 * Return: void
 */
void free_array(__attribute__((unused)) char **a)
{
	/*size_t i, l;

	l = _strlen(*a);
	for (i = 0; i < l; i++)
		free(a[i]);
	free(a);*/
}
